// src/services/emails.js

export function sendVerificationEmail(email) {
    console.log("📧 Simulando envío de email de verificación a:", email);
    console.log("Use verifyUserEmail(email) para verificar manualmente.");
}
